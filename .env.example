# ELION v5.9 Environment Configuration
# Copy to .env and fill in values

NODE_ENV=production
JWT_SECRET= # Generate with: openssl rand -hex 32
DATABASE_URL=postgresql://user:pass@postgres:5432/elion_db
REDIS_HOST=redis
REDIS_PORT=6379
CHROMA_URL=http://chroma:8000
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
MAPBOX_TOKEN=
POLICY_ENABLED=true
POLICY_DEFAULT=standard
POLICY_REQUIRE_SIGNOFF=true
SIMPLIFIED_MODE=false

